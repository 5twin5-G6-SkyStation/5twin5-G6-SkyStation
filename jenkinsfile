pipeline {
    agent any

    stages {
        stage('Getting project from Git') {
            steps {
                script {
                    checkout([$class: 'GitSCM', branches: [[name: 'widedgharbi-5twin5-G6']], userRemoteConfigs: [[url: 'https://github.com/5twin5-G6-SkyStation/5twin5-G6-SkyStation.git']]])
                }
            }
        }

        stage('Maven Clean & Compile') {
            steps {
                sh 'mvn clean compile -B -DskipTests'
            }
        }

       
stage('SonarQube Analysis') {
    steps {
        script {
            // Utiliser le nom d'utilisateur et le mot de passe corrects pour l'analyse SonarQube
            withSonarQubeEnv('admin', 'sonar') {
                sh 'mvn sonar:sonar'
            }
        }
    }
}
}
}
